(* Auto-generated from "parametric_types.atd" by atdml. *)

type 'a result =
  | Ok of 'a
  | Error of string
and ('a, 'b) either =
  | Left of 'a
  | Right of 'b
and all = ((int) result * (bool, string) either)

val result_of_yojson :
  (Yojson.Safe.t -> 'a) ->
  Yojson.Safe.t ->
  'a result
val yojson_of_result :
  ('a -> Yojson.Safe.t) ->
  'a result ->
  Yojson.Safe.t

val either_of_yojson :
  (Yojson.Safe.t -> 'a) ->
  (Yojson.Safe.t -> 'b) ->
  Yojson.Safe.t ->
  ('a, 'b) either
val yojson_of_either :
  ('a -> Yojson.Safe.t) ->
  ('b -> Yojson.Safe.t) ->
  ('a, 'b) either ->
  Yojson.Safe.t

val all_of_yojson : Yojson.Safe.t -> all
val yojson_of_all : all -> Yojson.Safe.t
val all_of_string : string -> all
val string_of_all : all -> string
val all_of_channel : in_channel -> all
val all_of_file : string -> all

--- ml ---
(* Auto-generated from "parametric_types.atd" by atdml. *)
[@@@ocaml.warning "-27-32-33-35-39"]

(* Inlined runtime — no external dependency needed. *)
module Atdml_runtime = struct
  let bad_type expected_type x =
    Printf.ksprintf failwith "expected %s, got: %s"
      expected_type (Yojson.Safe.to_string x)

  let bad_sum type_name x =
    Printf.ksprintf failwith "invalid variant for type '%s': %s"
      type_name (Yojson.Safe.to_string x)

  let missing_field type_name field_name =
    Printf.ksprintf failwith "missing field '%s' in object of type '%s'"
      field_name type_name

  let bool_of_yojson = function
    | `Bool b -> b
    | x -> bad_type "bool" x

  let yojson_of_bool b = `Bool b

  let int_of_yojson = function
    | `Int n -> n
    | x -> bad_type "int" x

  let yojson_of_int n = `Int n

  let float_of_yojson = function
    | `Float f -> f
    | `Int n -> Float.of_int n
    | x -> bad_type "float" x

  let yojson_of_float f = `Float f

  let string_of_yojson = function
    | `String s -> s
    | x -> bad_type "string" x

  let yojson_of_string s = `String s

  let unit_of_yojson = function
    | `Null -> ()
    | x -> bad_type "null" x

  let yojson_of_unit () = `Null

  let list_of_yojson f = function
    | `List xs -> List.map f xs
    | x -> bad_type "array" x

  let yojson_of_list f xs = `List (List.map f xs)

  let option_of_yojson f = function
    | `String "None" -> None
    | `List [`String "Some"; x] -> Some (f x)
    | x -> bad_type "option" x

  let yojson_of_option f = function
    | None -> `String "None"
    | Some x -> `List [`String "Some"; f x]

  let nullable_of_yojson f = function
    | `Null -> None
    | x -> Some (f x)

  let yojson_of_nullable f = function
    | None -> `Null
    | Some x -> f x
end

type 'a result =
  | Ok of 'a
  | Error of string
and ('a, 'b) either =
  | Left of 'a
  | Right of 'b
and all = ((int) result * (bool, string) either)

let rec result_of_yojson : 'a. (Yojson.Safe.t -> 'a) -> Yojson.Safe.t -> 'a result =
  fun of_yojson_a x ->
    match x with
    | `List [`String "Ok"; v] -> Ok (of_yojson_a v)
    | `List [`String "Error"; v] -> Error (Atdml_runtime.string_of_yojson v)
    | _ -> Atdml_runtime.bad_sum "result" x
and either_of_yojson : 'a 'b. (Yojson.Safe.t -> 'a) -> (Yojson.Safe.t -> 'b) -> Yojson.Safe.t -> ('a, 'b) either =
  fun of_yojson_a of_yojson_b x ->
    match x with
    | `List [`String "Left"; v] -> Left (of_yojson_a v)
    | `List [`String "Right"; v] -> Right (of_yojson_b v)
    | _ -> Atdml_runtime.bad_sum "either" x
and all_of_yojson (x : Yojson.Safe.t) : all =
  (fun x -> match x with | `List lst when List.length lst = 2 -> ((result_of_yojson Atdml_runtime.int_of_yojson) (List.nth lst 0), (either_of_yojson Atdml_runtime.bool_of_yojson Atdml_runtime.string_of_yojson) (List.nth lst 1)) | _ -> Atdml_runtime.bad_type "tuple" x) x

let rec yojson_of_result : 'a. ('a -> Yojson.Safe.t) -> 'a result -> Yojson.Safe.t =
  fun yojson_of_a x ->
    match x with
    | Ok v -> `List [`String "Ok"; yojson_of_a v]
    | Error v -> `List [`String "Error"; Atdml_runtime.yojson_of_string v]
and yojson_of_either : 'a 'b. ('a -> Yojson.Safe.t) -> ('b -> Yojson.Safe.t) -> ('a, 'b) either -> Yojson.Safe.t =
  fun yojson_of_a yojson_of_b x ->
    match x with
    | Left v -> `List [`String "Left"; yojson_of_a v]
    | Right v -> `List [`String "Right"; yojson_of_b v]
and yojson_of_all (x : all) : Yojson.Safe.t =
  (fun (x0, x1) -> `List [(yojson_of_result Atdml_runtime.yojson_of_int) x0; (yojson_of_either Atdml_runtime.yojson_of_bool Atdml_runtime.yojson_of_string) x1]) x

let all_of_string s =
  all_of_yojson (Yojson.Safe.from_string s)
let string_of_all x =
  Yojson.Safe.to_string (yojson_of_all x)
let all_of_channel ic =
  all_of_yojson (Yojson.Safe.from_channel ic)
let all_of_file path =
  all_of_yojson (Yojson.Safe.from_file path)

--- Input:
[ [ "Ok", 12 ], [ "Right", "a" ] ]
--- Output:
[ [ "Ok", 12 ], [ "Right", "a" ] ]
